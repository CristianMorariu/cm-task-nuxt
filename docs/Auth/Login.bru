meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{domain}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "login": "Admin",
    "password": "Password"
  }
}

settings {
  encodeUrl: true
}
