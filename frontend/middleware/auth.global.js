export default defineNuxtRouteMiddleware((to, from) => {
  // console.log(to);
  // console.log(from);
  // const isLoggedIn = false;
  // if (isLoggedIn) {
  // }
  //redirect to login
  // return navigateTo('login');
});
